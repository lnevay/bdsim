beam, particle = "proton",
      momentum = 100*GeV,
      distrType = "slowext",
      dTStart = 2.4*s,
      dTStop = -2.4*s,
      dPStart = 0,
      dPStop = 0.6*GeV;

! time start stop the wrong way around

include fodo.gmad;
include options.gmad;

option, ngenerate=10;