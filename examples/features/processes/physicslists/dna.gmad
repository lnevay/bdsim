c1: rcol, l=20*cm, outerDiameter=20*cm, material="water";

l1: line = (c1);

use, l1;

option, physicsList="em dna"; ! TODO why is em needed here to avoid a crash?

option, checkOverlaps=1;

beam, particle="e-",
      energy=520*keV;